import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',           // ← Рекомендую поменять на реальный
  appName: 'quran-voice-pwa',
  webDir: 'out',
  
  server: {
    androidScheme: 'https',           // важно для Next.js
    // iosScheme: 'https',            // можно добавить
    cleartext: true,                  // полезно на этапе разработки
  },

  // Дополнительные полезные настройки
  plugins: {
    SplashScreen: {
      launchShowDuration: 1000,
      backgroundColor: "#ffffff",
      showSpinner: true,
      spinnerColor: "#4f46e5",
    },
    StatusBar: {
      style: 'dark',
    },
  },

  // Для лучшей производительности PWA
  backgroundColor: '#ffffff',
};

export default config;